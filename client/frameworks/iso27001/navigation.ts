/**
 * ISO 27001 workspace sidebar — identical structure, labels, paths, and
 * permissions to NIST CSF (`nist-csf/navigation.ts`) so both frameworks
 * behave as one product; only routes under `basePath` differ in content.
 */
export { nistCsfNavigation as iso27001Navigation } from "../nist-csf/navigation";
